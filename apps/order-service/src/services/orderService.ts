import { Order } from "../model/order"
import { OrderInterface } from "../utils/interfaces"

export const addOrderService = async (data: OrderInterface) => {
    try {
        const { user, paymetStatus, shippingAddress, items, price } = data

        const 

    }
}